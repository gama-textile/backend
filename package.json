{
  "name": "gt-react",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon ./bin/www",
    "test": "fresh migrate seed && nyc mocha --reporter mocha-simple-html-reporter --reporter-options output=test-report.html --exit && live-server test-report.html"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.2.1",
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "fresh_sequelize": "^1.0.2",
    "http-errors": "~1.6.2",
    "live-server": "^1.2.1",
    "morgan": "~1.9.0",
    "mysql2": "^1.6.5",
    "sequelize": "^4.42.0"
  },
  "devDependencies": {
    "mocha": "^6.0.1",
    "mocha-simple-html-reporter": "^1.1.0",
    "nyc": "^13.3.0"
  }
}
